/**
  * Created by preethisrinivasan on 15/07/18.
  */
import example.Lists
object Main extends App{
  println(s"Sum of the list is : ${Lists.sum(List(1,2,3))}") // String extrapolation
  println(s"Max in a list is : ${Lists.max(List(1,2,3))}")
}
